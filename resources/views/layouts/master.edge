<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title></title>
  <script src="https://unpkg.com/unpoly@2.6.1/unpoly.min.js"></script>
  <link rel="stylesheet" href="https://unpkg.com/unpoly@2.6.1/unpoly.min.css">
  @entryPointStyles('app')
  @entryPointScripts('app')
</head>
<body>

  <section>
    <div class="sidebar-wrapper">
      <aside class="sidebar" layout-sidebar>
        @if(up.targetIncludes('[layout-sidebar]'))
          <ul>
            @each(joke in await loadJokes())
              <li>
                <a up-target="[layout-main]" href="{{ route('JokesController.show', [joke.id]) }}">{{ joke.name }}</a>
              </li>
            @end
          </ul>
        @end

        <div>
          <a up-target="[layout-main]" href="{{ route('JokesController.create') }}">Add your joke</a>
        </div>
      </aside>
    </div>

    <main class="main" up-main layout-main>
      @!section('main')
    </main>
  </section>

</body>
</html>
